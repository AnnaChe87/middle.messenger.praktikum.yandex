<div class="login column">
    <div class="login-header">
        <h2>Вход</h2>
    </div>
    <div class="login-content">
        <form>
            {{#each controls}}
                {{{this}}}
            {{/each}}
        </form>
    </div>
    <div class="login-footer column">
        {{{btn}}}
        {{{link}}}
    </div>
</div>
